# Netlify configuration file
[build]
  publish = "./dist"
  command = "npm run build"

[dev]
  command = "npm run dev"
  port = 8888

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
